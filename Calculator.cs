namespace DLLExample
{
    public class Calculator
    {
        public static int squareCalculate(int number)
        {
            return (number * number);
        }

        public static int cubeCalculate(int number)
        {
            return (number * number * number);
        }
    }
}